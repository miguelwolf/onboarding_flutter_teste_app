import 'package:flutter/material.dart';

class PagamentoPage extends void StatelessWidget {
  const PagamentoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pagamento'),
      ),
      body: const SizedBox.shrink(),
    );
  }
}